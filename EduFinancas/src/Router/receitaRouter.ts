// import { Request, Response } from 'express';
// import express from "express";
// import { ReceitaController } from './../Controller/receitaController';

// const receitaController = new ReceitaController();
// const routerReceita = express.Router();
// routerReceita.use(express.json());

// routerReceita.get("/receita/:receitaId", receitaController.getReceitaById.bind(receitaController))
// routerReceita.get("/receita"), receitaController.getAllReceita.bind(receitaController)
// routerReceita.post("/receita/:userId", receitaController.createReceita.bind(receitaController))
// routerReceita.delete("/receita/:receitaId/user/:userId", receitaController.deleteReceita.bind(receitaController))
// routerReceita.put("/receita/:receitaId/user/:userId", receitaController.updateReceita.bind(receitaController))

// export default routerReceita;